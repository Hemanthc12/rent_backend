{
  "name": "rent-manager-app",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper for the Rent Manager web app (built with Capacitor)",
  "scripts": {
    "build:android": "npx cap add android && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
